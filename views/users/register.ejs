<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,500;0,700;1,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/stylesheet_register.css">
    <script src="https://kit.fontawesome.com/10efa87d39.js" crossorigin="anonymous"></script>
    <title>Jobbok-registrar</title>
</head>
<body>


    <!-- Header -->
    <%- include('../partials/header.ejs') %> 

    <!-- Navbar categorias -->
    <%- include('../partials/navbarCategorias.ejs') %> 

    <form class="formularioLogin" action="/usuario/register" method="POST">
        <h2>Únete a Jobbook</h2>
        <label class="contenedorInput">
            <p class="tituloInput">Nombre completo</p>
            <input type="text" name="nombre" placeholder="Pepito Perez Gonzales" class="input"
            value="<%= locals.oldData && oldData.nombre ? oldData.nombre : null %>" >
            <% if (locals.mensajesError && mensajesError.nombre) { %>
            <p class="mensajeError"><%= mensajesError.nombre.msg %></p>
            <% } %>
        </label>

        <label class="contenedorInput">
            <p class="tituloInput">E-mail</p>
            <input type="email" name="correo" placeholder="user@example.com" class="input"
            value="<%= locals.oldData && oldData.correo ? oldData.correo : null %>" >
            <% if (locals.mensajesError && mensajesError.correo) { %>
            <p class="mensajeError"><%= mensajesError.correo.msg %></p>
            <% } %>
        </label>

        <label class="contenedorInput">
            <p class="tituloInput">Usuario</p>
            <input type="text" name="nombrePersonalizado" placeholder="pepigon" class="input"
            value="<%= locals.oldData && oldData.nombrePersonalizado ? oldData.nombrePersonalizado : null %>">
            <% if (locals.mensajesError && mensajesError.nombrePersonalizado) { %>
            <p class="mensajeError"><%= mensajesError.nombrePersonalizado.msg%></p>
            <% } %>
        </label>
        <label class="contenedorInput">
            <p class="tituloInput">Contraseña</p>
            <input type="password" name="password" placeholder="*********" class="input">
            <% if (locals.mensajesError && mensajesError.password) { %>
            <p class="mensajeError"><%= mensajesError.password.msg  %></p>
            <% } %>
        </label>
        <label class="contenedorInput">
            <p class="tituloInput">Confirmar contraseña</p>
            <input type="password" name="confirmarContraseña" placeholder="*********" class="input">
            <% if (locals.mensajesError && mensajesError.confirmarContraseña) { %>
            <p class="mensajeError"><%= mensajesError.confirmarContraseña.msg %></p>
            <% } %>
        </label>
        <label class="contenedorTerminos">
            <input type="checkbox" name="terminos" class="">
            <p>Acepto terminos de servicios y politica de privacidad</p>
            <% if (locals.mensajesError && mensajesError.terminos) { %>
            <p class="mensajeError"><%= mensajesError.terminos.msg  %></p>
            <% } %>
        </label>
        
        <button class="boton">Registrarme</button>
        
        <p>¿Ya eres miembro? <a href="">Iniciar sesión</a></p>
    </form>

    <!-- Footer -->
    <%- include('../partials/footer.ejs') %>
</body>

</html>